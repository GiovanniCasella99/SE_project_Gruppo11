spring.application.name=seproject

# Path where rules are persisted (relative to working directory)
rules.storage.path=rules.json

# Rule evaluation interval in milliseconds (default: 10s)
rule.check.interval=10000
